# Portfolio Dashboard

React + Vite portfolio dashboard prepared for deployment.

## Deploy
Import this repository into Vercel. Vercel should automatically detect Vite and use:

- Build command: `npm run build`
- Output directory: `dist`
